"""Game URL routes.

Routes are added as the web layer is built (auth, home, world, combat, etc.).
For now this is intentionally empty so the project runs while we scaffold.
"""
from django.urls import path

app_name = "game"

urlpatterns: list[path] = []
